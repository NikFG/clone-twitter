import React from 'react';
import {Icon, InlineIcon} from '@iconify/react';
import repeatVariant from '@iconify-icons/mdi/repeat-variant';

export const RetIcon: React.FC = (props) => {
    return (
        <Icon icon={repeatVariant}/>
    );
};
